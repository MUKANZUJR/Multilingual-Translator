# Multilingual-Translator
A full-stack style web translation application developed using vanilla HTML, CSS, and JavaScript. It consumes the MyMemory Translation REST API via asynchronous fetch requests, implements real-time translation with debounce, language swapping, text-to-speech, clipboard API, persistent dark mode, responsive UI.
